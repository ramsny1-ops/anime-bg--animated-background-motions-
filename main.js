const search=document.querySelector('#effectSearch');const category=document.querySelector('#categoryFilter');const cards=[...document.querySelectorAll('.effect-card')];function filter(){const q=search.value.trim().toLowerCase();const c=category.value;cards.forEach(card=>{const text=(card.dataset.title+' '+card.textContent).toLowerCase();const okText=!q||text.includes(q);const okCat=c==='all'||card.dataset.category===c;card.hidden=!(okText&&okCat);});}search.addEventListener('input',filter);category.addEventListener('change',filter);