# Integration examples

These examples show patterns for reusing anime-bg effects in real pages.

- `hero-background.html`: decorative animation behind hero content;
- `configuration-example.js`: changing a copied effect through configuration;
- `css-layering.css`: safe content and canvas layering patterns.

The effect folders remain the canonical source. Examples intentionally stay small.
